name: Deploy Backend

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'RAG/**'
      - 'scripts/**'
      - 'config/**'
      - 'infra/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  deploy-backend:
    name: Deploy to Backend Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Sync to Backend Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.BACKEND_HOST }}
          username: ${{ secrets.BACKEND_USER }}
          key: ${{ secrets.BACKEND_SSH_KEY }}
          port: ${{ secrets.BACKEND_SSH_PORT || 22 }}
          script: |
            set -e
            echo "🔄 백엔드 서버 배포 시작..."
            
            # LIMEN 디렉토리 확인 및 이동
            if [ ! -d "/home/darc0/LIMEN" ]; then
              echo "❌ 오류: /home/darc0/LIMEN 디렉토리가 존재하지 않습니다"
              exit 1
            fi
            cd /home/darc0/LIMEN || exit 1
            
            # 현재 브랜치 확인
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
            echo "📋 현재 브랜치: $CURRENT_BRANCH"
            
            # Git pull (최신 코드 가져오기) - 백엔드 우선순위
            echo "⬇️  최신 코드 가져오는 중 (백엔드 우선순위)..."
            git fetch origin || {
              echo "⚠️  git fetch 실패, 계속 진행..."
            }
            # 항상 최신 main 브랜치로 강제 업데이트
            git reset --hard origin/main || {
              echo "❌ git reset 실패"
              exit 1
            }
            git clean -fd || {
              echo "⚠️  git clean 실패, 계속 진행..."
            }
            echo "✅ 백엔드 서버가 최신 상태로 업데이트됨"
            
            # RAG 인덱싱 실행
            if [ -f "scripts/rag-index.sh" ]; then
              echo "🔄 RAG 인덱싱 실행 중..."
              chmod +x scripts/rag-index.sh
              ./scripts/rag-index.sh --auto || true
            fi
            
            # 백엔드 서비스 재시작
            if command -v pm2 &> /dev/null; then
              echo "🔄 백엔드 서비스 재시작 중..."
              cd /home/darc0/LIMEN/backend
              pm2 restart limen --update-env || true
            fi
            
            echo "✅ 백엔드 서버 배포 완료"
      
      - name: Notify Backend Deploy Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ 백엔드 서버 배포 성공"
          else
            echo "❌ 백엔드 서버 배포 실패"
            echo "📋 오류 분석:"
            echo "   - GitHub Secrets 확인 필요 (BACKEND_HOST, BACKEND_USER, BACKEND_SSH_KEY)"
            echo "   - 서버 경로 확인: /home/darc0/LIMEN"
            echo "   - SSH 연결 확인 필요"
          fi
