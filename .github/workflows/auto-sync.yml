name: Auto Sync to Servers

on:
  push:
    branches:
      - main
    # 모든 변경사항에 대해 동기화 (프론트/백엔드 각각 우선순위 적용)
  workflow_dispatch:

jobs:
  sync-backend:
    name: Sync to Backend Server
    runs-on: ubuntu-latest
    # 백엔드 변경사항이 있거나, 모든 변경사항에 대해 항상 동기화
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && (
        contains(github.event.head_commit.message, 'backend') ||
        contains(github.event.head_commit.message, 'Backend') ||
        contains(github.event.head_commit.message, 'BACKEND') ||
        contains(join(github.event.commits.*.message, ' '), 'backend') ||
        contains(join(github.event.commits.*.modified, ' '), 'backend/') ||
        contains(join(github.event.commits.*.added, ' '), 'backend/') ||
        true  # 항상 실행 (모든 변경사항에 대해 최신 상태 유지)
      ))
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Sync to Backend Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.BACKEND_HOST }}
          username: ${{ secrets.BACKEND_USER }}
          key: ${{ secrets.BACKEND_SSH_KEY }}
          port: ${{ secrets.BACKEND_SSH_PORT || 22 }}
          script: |
            set -e
            echo "🔄 백엔드 서버 동기화 시작..."
            
            # LIMEN 디렉토리 확인 및 이동
            if [ ! -d "/home/darc0/LIMEN" ]; then
              echo "❌ 오류: /home/darc0/LIMEN 디렉토리가 존재하지 않습니다"
              exit 1
            fi
            cd /home/darc0/LIMEN || exit 1
            
            # 현재 브랜치 확인
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
            echo "📋 현재 브랜치: $CURRENT_BRANCH"
            
            # Git pull (최신 코드 가져오기) - 백엔드 우선
            echo "⬇️  최신 코드 가져오는 중 (백엔드 우선)..."
            git fetch origin || {
              echo "⚠️  git fetch 실패, 계속 진행..."
            }
            
            # 백엔드 코드 우선 동기화
            echo "📦 백엔드 코드 동기화 중..."
            git checkout origin/main -- backend/ || {
              echo "⚠️  backend/ 체크아웃 실패, 전체 동기화 시도..."
              git reset --hard origin/main || git reset --hard origin/$CURRENT_BRANCH || {
                echo "❌ git reset 실패"
                exit 1
              }
            }
            
            # RAG 문서 동기화 (항상 최신)
            echo "📚 RAG 문서 동기화 중..."
            git checkout origin/main -- RAG/ || {
              echo "⚠️  RAG/ 체크아웃 실패"
            }
            
            # 나머지 필수 파일들 동기화
            git checkout origin/main -- scripts/ config/ infra/ .github/ || true
            
            git clean -fd || {
              echo "⚠️  git clean 실패, 계속 진행..."
            }
            
            # RAG 인덱싱 실행
            if [ -f "scripts/rag-index.sh" ]; then
              echo "🔄 RAG 인덱싱 실행 중..."
              chmod +x scripts/rag-index.sh
              ./scripts/rag-index.sh --auto || true
            fi
            
            # 백엔드 서비스 재시작 (선택적)
            if command -v pm2 &> /dev/null; then
              echo "🔄 백엔드 서비스 재시작 중..."
              cd /home/darc0/LIMEN/backend
              pm2 restart limen --update-env || true
            fi
            
            echo "✅ 백엔드 서버 동기화 완료"
      
      - name: Notify Backend Sync Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ 백엔드 서버 동기화 성공"
          else
            echo "❌ 백엔드 서버 동기화 실패"
            echo "📋 오류 분석:"
            echo "   - GitHub Secrets 확인 필요 (BACKEND_HOST, BACKEND_USER, BACKEND_SSH_KEY)"
            echo "   - 서버 경로 확인: /home/darc0/LIMEN"
            echo "   - SSH 연결 확인 필요"
          fi

  sync-frontend:
    name: Sync to Frontend Server
    runs-on: ubuntu-latest
    # 프론트엔드 변경사항이 있거나, 모든 변경사항에 대해 항상 동기화
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && (
        contains(github.event.head_commit.message, 'frontend') ||
        contains(github.event.head_commit.message, 'Frontend') ||
        contains(github.event.head_commit.message, 'FRONTEND') ||
        contains(join(github.event.commits.*.message, ' '), 'frontend') ||
        contains(join(github.event.commits.*.modified, ' '), 'frontend/') ||
        contains(join(github.event.commits.*.added, ' '), 'frontend/') ||
        true  # 항상 실행 (모든 변경사항에 대해 최신 상태 유지)
      ))
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Sync to Frontend Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.FRONTEND_HOST }}
          username: ${{ secrets.FRONTEND_USER }}
          key: ${{ secrets.FRONTEND_SSH_KEY }}
          port: ${{ secrets.FRONTEND_SSH_PORT || 22 }}
          script: |
            set -e
            echo "🔄 프론트엔드 서버 동기화 시작..."
            
            # LIMEN 디렉토리 확인 및 이동
            if [ ! -d "/home/darc/LIMEN" ]; then
              echo "❌ 오류: /home/darc/LIMEN 디렉토리가 존재하지 않습니다"
              exit 1
            fi
            cd /home/darc/LIMEN || exit 1
            
            # 현재 브랜치 확인
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
            echo "📋 현재 브랜치: $CURRENT_BRANCH"
            
            # Git pull (최신 코드 가져오기) - 프론트엔드 우선
            echo "⬇️  최신 코드 가져오는 중 (프론트엔드 우선)..."
            git fetch origin || {
              echo "⚠️  git fetch 실패, 계속 진행..."
            }
            
            # 프론트엔드 코드 우선 동기화
            echo "📦 프론트엔드 코드 동기화 중..."
            git checkout origin/main -- frontend/ || {
              echo "⚠️  frontend/ 체크아웃 실패, 전체 동기화 시도..."
              git reset --hard origin/main || git reset --hard origin/$CURRENT_BRANCH || {
                echo "❌ git reset 실패"
                exit 1
              }
            }
            
            # RAG 문서 동기화 (항상 최신)
            echo "📚 RAG 문서 동기화 중..."
            git checkout origin/main -- RAG/ || {
              echo "⚠️  RAG/ 체크아웃 실패"
            }
            
            # 나머지 필수 파일들 동기화
            git checkout origin/main -- scripts/ config/ infra/ .github/ || true
            
            git clean -fd || {
              echo "⚠️  git clean 실패, 계속 진행..."
            }
            
            # RAG 인덱싱 실행
            if [ -f "scripts/rag-index.sh" ]; then
              echo "🔄 RAG 인덱싱 실행 중..."
              chmod +x scripts/rag-index.sh
              ./scripts/rag-index.sh --auto || true
            fi
            
            # 프론트엔드 서비스 재시작 (선택적)
            if command -v pm2 &> /dev/null; then
              echo "🔄 프론트엔드 서비스 재시작 중..."
              cd /home/darc/LIMEN/frontend
              pm2 restart limen-frontend --update-env || true
            fi
            
            echo "✅ 프론트엔드 서버 동기화 완료"
      
      - name: Notify Frontend Sync Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ 프론트엔드 서버 동기화 성공"
          else
            echo "❌ 프론트엔드 서버 동기화 실패"
            echo "📋 오류 분석:"
            echo "   - GitHub Secrets 확인 필요 (FRONTEND_HOST, FRONTEND_USER, FRONTEND_SSH_KEY)"
            echo "   - 서버 경로 확인: /home/darc/LIMEN"
            echo "   - SSH 연결 확인 필요"
          fi

