name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'RAG/**'
      - 'scripts/**'
      - 'config/**'
      - 'infra/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Deploy to Frontend Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Sync to Frontend Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.FRONTEND_HOST }}
          username: ${{ secrets.FRONTEND_USER }}
          key: ${{ secrets.FRONTEND_SSH_KEY }}
          port: ${{ secrets.FRONTEND_SSH_PORT || 22 }}
          script: |
            set -e
            echo "🔄 프론트엔드 서버 배포 시작..."
            
            # LIMEN 디렉토리 확인 및 이동
            if [ ! -d "/home/darc/LIMEN" ]; then
              echo "❌ 오류: /home/darc/LIMEN 디렉토리가 존재하지 않습니다"
              exit 1
            fi
            cd /home/darc/LIMEN || exit 1
            
            # 현재 브랜치 확인
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
            echo "📋 현재 브랜치: $CURRENT_BRANCH"
            
            # Git pull (최신 코드 가져오기) - 프론트엔드 우선순위
            echo "⬇️  최신 코드 가져오는 중 (프론트엔드 우선순위)..."
            git fetch origin || {
              echo "⚠️  git fetch 실패, 계속 진행..."
            }
            # 항상 최신 main 브랜치로 강제 업데이트 (한 몸)
            git reset --hard origin/main || {
              echo "❌ git reset 실패"
              exit 1
            }
            git clean -fd || {
              echo "⚠️  git clean 실패, 계속 진행..."
            }
            echo "✅ 프론트엔드 서버가 최신 상태로 업데이트됨 (프론트엔드 우선순위)"
            
            # RAG 인덱싱 실행
            if [ -f "scripts/rag-index.sh" ]; then
              echo "🔄 RAG 인덱싱 실행 중..."
              chmod +x scripts/rag-index.sh
              ./scripts/rag-index.sh --auto || true
            fi
            
            # 프론트엔드 서비스 재시작
            if command -v pm2 &> /dev/null; then
              echo "🔄 프론트엔드 서비스 재시작 중..."
              cd /home/darc/LIMEN/frontend
              pm2 restart limen-frontend --update-env || true
            fi
            
            echo "✅ 프론트엔드 서버 배포 완료"
      
      - name: Notify Frontend Deploy Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ 프론트엔드 서버 배포 성공"
          else
            echo "❌ 프론트엔드 서버 배포 실패"
            echo "📋 오류 분석:"
            echo "   - GitHub Secrets 확인 필요 (FRONTEND_HOST, FRONTEND_USER, FRONTEND_SSH_KEY)"
            echo "   - 서버 경로 확인: /home/darc/LIMEN"
            echo "   - SSH 연결 확인 필요"
          fi
