/**
 * lib/types/index.ts 테스트
 */

import * as types from '../index'

describe('lib/types/index', () => {
  it('exports types', () => {
    // types 파일이 export되는지 확인
    expect(types).toBeDefined()
  })
})



